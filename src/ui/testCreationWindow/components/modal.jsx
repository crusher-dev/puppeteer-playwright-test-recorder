import {  Component, React } from 'preact';

import Portal from 'preact-portal';

export class Modal {

    render() {
        return (
            <Portal into="#modal">
               dffd
            </Portal>
        );
    }
}
